import React, { PropsWithChildren } from 'react'

export const metadata = {
    title: 'Admin Dashboard - GolfStrokePool',
}

export default function AdminLayout({ children }: PropsWithChildren) {
    return (
        children
    )
}
